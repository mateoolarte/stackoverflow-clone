<div class="container">
  <div class="row">

    <% if @question.errors.any? %>
    <div class="col-md-6 offset-md-3 mt-5">
      <ul>
        <% @question.errors.full_messages.each do |msg| %>
          <li class="text-danger"><%= msg %></li>
        <% end %>
      </ul>
    </div>
    <% end %>

    <div class="col-md-6 offset-md-3 <%= @question.errors.any? ? "" : "mt-5" %>">
      <h1 class="mb-4">Crear pregunta</h1>
      <%= form_for @question do |f| %>
        <%= f.hidden_field :user_id, :value => current_user.id %>
        <div class="form-group">
          <%= f.text_field :title, class: "form-control", placeholder: "Título" %>
        </div>
        <div class="form-group">
          <%= f.text_area :description, size: "25x8", class: "form-control", placeholder: "Descripción" %>
        </div>
        <div>
          <%= f.submit "Publicar Producto", class: "btn btn-primary btn-lg btn-block" %>
        </div>
      <% end %>
    </div>
  </div>
</div>